{# <div class="meeting-com {{ content.class_odd_even }}">
   {#  <div class="date-meet">
        <span class="day">{{ content.start_date.day }}</span>
        <span class="month-and-year">{{ content.start_date.month }} {{  '  ' ~ content.start_date.year  }}</span>
    </div>     #}
      {# <div class="title-meet">
        <span class="title-meet-commission"><a href="/civicrm-event/{{ content.event_id }}">{{ content.title }}</a></span>
        {# <span class="hour-begining-meet-commission">Début : </span>
        <span class="hourss">{{ content.start_date.hour ~ 'h ' ~ content.start_date.minute}}</span> #}
   {#  </div>
</div> #}


<a href="/civicrm-event/{{ content.event_id }}">
<div class="meeting-com {{ content.class_odd_even }}">
    <div class="date-meet">
        <span class="day">{{ content.day }}</span>
        <span class="month-and-year">{{ content.month }} {{  '  ' ~ content.year  }}</span>
        <div class="debuth">
            <span class="hourss">{{ content.hour_complet }}</span>
        </div>
    </div>     
     <div class="title-meet">
        <span class="field_sous_titre_slide aaa">{{ content.title }}</span>
        
    </div>
</div></a>